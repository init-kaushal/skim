# skim — handoff / continuation notes

**Date:** 2026-09-14
**Purpose:** Full context so a fresh Claude Code session (or a human) can pick this project up with zero prior context. This file exists because the SDD execution ledger and per-task review packages live under `.superpowers/sdd/` (gitignored, worktree-local) and do not travel with a `git push`. Everything load-bearing from that ledger is reproduced here.

## Where things live

- **Spec (authoritative design):** `docs/superpowers/specs/2026-09-07-skim-design.md`
- **Implementation plan (15 tasks):** `docs/superpowers/plans/2026-09-08-skim.md`
- **Hook-contract spike findings:** `docs/superpowers/notes/2026-09-08-hook-contract.md`
- **This file:** `docs/superpowers/notes/2026-09-14-handoff.md`
- **Active branch:** `skim-impl` (not yet merged to `main` — see "What's pending" below)
- **Repo:** `github.com/init-kaushal/skim` (private)

## What skim is

A Claude Code plugin (single Go binary + plugin scaffolding) that intercepts likely-expensive `Read`/`Grep`/`Bash` tool calls via `PreToolUse` hooks and substitutes a compact Haiku-produced digest, so bulk content never lands in — or gets re-sent with — the main session's context. It works regardless of which model drives the session (only the worker is pinned to Haiku), reproduces the "Portal by Spotify / shunt" token-saving pattern with no external platform, and fails open (allows the original tool call) on any internal error. Read the spec §1 for the full framing.

## Status: implementation + all known fixes complete; last fix is controller-verified but not independently re-reviewed

All 15 plan tasks are implemented, individually task-reviewed (spec compliance + code quality, with fix loops where needed), and committed. On top of that, four rounds of bug-fixing happened after implementation — the session was stopped here at the user's request ("stop all background tasks and push") before dispatching a fifth review round, so read point 4 carefully.

1. **Fence-fix** (found via a live smoke test against the real Anthropic API, not simulated): Haiku wraps its JSON responses in a markdown code fence despite being told "no prose," which made every real interception silently fail to parse and degrade open — i.e. skim did nothing in real use, though safely. Fixed in `internal/worker` (strips the fence before handing bytes to `internal/digest`'s parsers). Task-reviewed clean.

2. **Final whole-branch review fix wave**: a full-branch review (dispatched on Opus) found **4 Critical + 8 Important** cross-cutting bugs that no single task's review could see — see "Final review findings" below. All 15 were fixed in one wave (5 commits, `4132e04..1365ea0`). A scoped re-review of that wave (also Opus) came back **14/15 ADDRESSED**, with one item — Grep's `output_mode` handling — only **partially** addressed (see next).

3. **Small targeted fix wave** (commit `58d2776`): fixed the I3 remainder plus two Minors the same re-review reproduced as actually harmful:
   - Grep-hook now treats an omitted `output_mode` (`OutputMode == ""`) the same as `"files_with_matches"` — Claude Code itself defaults the Grep tool's `output_mode` to `"files_with_matches"` when the model omits it, so `PreToolUse` was seeing the raw empty value and falsely intercepting every already-bounded call (`internal/handler/grep.go:36`).
   - Bash-hook's `skim run --`/`skim run -- sh -c` suggestions now shell-quote the resolved self-path when it contains whitespace (`quoteSelfIfNeeded` in `internal/handler/selfpath.go`), and `isSkimCommand` now recognizes that quoted form — closing a spaced-install-path re-opening of C1's infinite-escalation bug. Verified against an actual binary copied to a spaced path (`/private/tmp/skim test dir/skim`): deny → suggestion → fed back → allow.
   - `\n`/`\r` added to `shellMetaChars` — a multi-line Bash command now correctly takes the `sh -c '...'`-wrapped suggestion form instead of silently losing every line but the first.

   A scoped re-review of this commit found it 2/3 clean, with Fix 2 (spaced self-path) only **partially** addressed: the Bash-hook's `skim run --` suggestion was fixed and verified, but the Read-hook's independent `skim cat` escape hatch still passed the raw, unquoted self-path (same bug, second path).

4. **Final one-line fix** (commit after `58d2776`, current `HEAD` — check `git log -1` for the exact SHA): `internal/handler/read.go`'s call to `digest.RenderFileMap` now passes `quoteSelfIfNeeded(execPath())` instead of the raw `execPath()`, closing the gap point 3 found. A real test (`TestReadHook_SpacedSelfPath_QuotesFull` in `internal/handler/read_test.go`) uses the same injectable `executablePath` seam as the Bash-hook's spaced-path test and asserts the "force full file" suggestion is properly quoted. **The controller (this session) verified this directly** — read the diff, confirmed it's exactly the one-line change plus a genuine test, confirmed `isSkimCommand`'s quoted-prefix fallback already handles the `cat` verb (`internal/handler/selfpath.go:55,68` — no further code change needed there, as the prior re-review predicted), and ran `go build ./... && go vet ./... && gofmt -l . && go test ./...` — all clean. **This fix was NOT independently re-reviewed by a fresh subagent** (the user asked to stop background tasks before that dispatch happened) and the fix was not empirically re-verified against an actual spaced-path binary copy (only via the unit test's injectable seam) — the earlier re-review had asked for that real-binary check too. If you want the same rigor the other three fix waves got, run one more scoped review of just this commit before merging. Given the fix is a one-line, mechanical, already-proven-pattern change reusing an existing tested helper, this is very likely fine as-is — but it hasn't had a second set of eyes.

### Commit history on `skim-impl` (chronological, oldest first)

```
docs: record Claude Code PreToolUse hook contract findings (spike)
chore: scaffold skim module and subcommand dispatch                         [Task 2]
feat: config loading, defaults, validation, not-active precedence           [Task 3]
feat: detect package for size thresholds and command/path pattern matching  [Task 4]
feat: hookio for PreToolUse stdin parsing and allow/deny decisions          [Task 5]
feat: digest schemas, parsers, and compact renderers                        [Task 6]
feat: worker package — nested claude -p invocation with timeout...          [Task 7]
feat: content-hashed digest cache with TTL sweep                            [Task 8]
feat: metrics — JSONL interception log and aggregation                      [Task 9]
feat: handler.Deps and Read interception with cache, degradation, metrics   [Task 10]
fix: readCapped drains up to the cap instead of a single Read               [Task 10 fix round]
feat: Grep interception with cluster digest                                 [Task 11]
feat: Bash interception redirects noisy commands to 'skim run'...           [Task 12]
feat: cli commands — doctor, stats, config, cat                             [Task 13]
feat: wire real dependencies into hook and command dispatch...              [Task 14]
feat: plugin packaging, code-writer agent+skill, README                     [Task 15]
fix: strip markdown code fences from worker JSON results                    [fence-fix]
fix: stop skim's Bash escape hatches from defeating themselves              [C1,C2,C3,I5]
fix: never send binaries and images through the digest path                 [C4]
fix: scope, defer and surface the Grep sample; skip already-bounded calls   [I1,I2,I3]
fix: restrict the worker to zero tools and meter what it costs              [I4-A,I6]
fix: label digests as data, declare a hook timeout, short-circuit switches  [I4-B,I7,I8,P1-P3]
fix: marketplace.json owner + README install line -> init-kaushal           [docs handoff]
fix: grep output_mode default, spaced self-path escalation, newline meta    [small-fix wave]
fix: quote self-path in Read hook's "force full file" suggestion too       [final one-liner, HEAD]
```

## Final review findings and how each was fixed

The final whole-branch review (`docs/superpowers/plans/2026-09-08-skim.md`'s process — see `superpowers:subagent-driven-development`) found:

**Critical (all empirically verified against the real built binary before fixing):**
- **C1** — The Bash deny message told the model to run `skim run -- <original command>`, which still matched the same noisy pattern (`\bcat\s`, `go test`, etc.) and got denied again — infinite escalation, the Bash path never completed. **Fixed:** `BashHook` now allows unconditionally when a command already invokes skim itself.
- **C2** — Both escape hatches (`skim run`, `skim cat` suggestions) told the model to run a bare `skim`, which isn't on `PATH` (the binary lives at `plugin/bin/skim`, invoked via `${CLAUDE_PLUGIN_ROOT}/bin/skim`). **Fixed:** both now resolve and interpolate `os.Executable()`'s absolute path.
- **C3** — `/skim off` / `/skim on` (the README's and `plugin/commands/skim.md`'s advertised primary kill switch) weren't dispatched in `cmd/skim/main.go` — only `skim config off` worked. **Fixed:** added the dispatch case.
- **C4** — `Read` interception had no binary/image gate — a 200KB+ PNG or PDF got denied and up to 400KB of raw binary shipped into a Haiku prompt. **Fixed:** `internal/detect` gained a binary/image detector (extension allowlist + NUL-byte sniff), checked before size-threshold interception.

**Important:**
- **I1/I2** — Grep's sample `rg` call dropped `--glob`/`--type` (described the wrong scope) and ran unconditionally even when the call would pass through (2x rg cost on every single Grep). **Fixed:** shared scope-filter args; sample is now lazy (only computed after confirming the match count exceeds threshold) — required splitting `handler.Deps.CountMatches` into a count-only function plus a lazy `Deps.Sample`.
- **I3** — Grep ignored `output_mode`/`head_limit`, so already-cheap calls (`output_mode: count`, small `head_limit`) got needlessly intercepted. **Fixed:** `hookio.GrepInput` now parses `head_limit`; `GrepHook` passes through for `count`/`files_with_matches` output modes or a small explicit head limit.
- **I4-A** — The worker wasn't actually tool-restricted despite spec §4.4 saying "no tools." **Fixed:** `--tools ""` verified empirically to work (worker replied "NOTOOLS" when asked to run Bash; cached system-prompt tokens dropped from 10446 to 5579).
- **I4-B** — Untrusted digest content (e.g. from an adversarial repo file) reached the main model unlabeled inside `permissionDecisionReason`. **Fixed:** digests are now prefixed with a framing line marking them as data, not instructions.
- **I5** — `skim run -- <cmd>` doesn't use a shell, so a suggested compound command (pipes, `&&`, etc.) got silently re-parsed by the *outer* shell with different meaning; also, wrapping changes what string Claude Code's Bash permission system sees. **Fixed:** commands containing shell metacharacters are now suggested as `skim run -- sh -c '<escaped>'`; README documents the permission-matching caveat.
- **I6** — `metrics.Entry.WorkerTokens` was declared but never populated, so `skim stats` could never show the Haiku-cost side of the ledger — exactly the question spec §9 designates `skim stats` to answer. **Fixed:** `worker.Run`'s signature now also returns token usage parsed from the real `claude -p --output-format json` envelope; threaded through every call site; summed in `Aggregate`; printed by `skim stats`.
- **I7** — `SKIM_ACTIVE`/`SKIM_DISABLED` were checked *after* a config load + full cache-directory sweep on every hook call, not first as spec §4.0 requires. **Fixed:** an early-exit check for these two env vars now runs before anything else in `hookMain`.
- **I8** — No hook timeout declared in `hooks.json`; `worker_timeout_sec` (45s default) was observed borderline. **My original fix instruction (relayed from the reviewer) assumed Claude Code's hook timeout default was ~60s and told the implementer to lower `worker_timeout_sec` to 30. The implementer instead found the real documented default is 600s for command hooks, and empirically showed 30s causes real degrade-open timeouts on a legitimate ~92KB file (30.06s) while 45s left real margin (21.9s–34.8s observed).** They kept the default at 45, added an explicit self-imposed `"timeout": 60` per hook entry in `hooks.json` (tighter than the platform default, for predictability), and kept `Validate()`'s `>55` ceiling binding against that 60s bound. **This deviation was reviewed and accepted as correct** — it's evidence-backed and the premise it corrects was mine, not the implementer's error.

**Promoted from deferred minors (treated as required):**
- **P1** — Added a `Deny` round-trip test with a quote/newline-containing reason (relevant now that C1's fix means deny reasons routinely contain multi-line suggested commands).
- **P2** — Deleted `TestRun_SetsSkimActiveForChild`, which asserted nothing and had a comment describing fake behavior the fake didn't actually have.
- **P3** — Read/Grep tests now capture and assert the recorded `metrics.Entry`'s fields (this is what would have caught I6 originally — no test ever looked inside an `Entry`).

### Worth your attention (not a defect, a real signal)

The fix wave's live smoke test on a 3191-line/92423-byte real file reported: **saved ~22,556 tokens, worker spent ~86,905 tokens, net ~−64,349**. The worker cost *exceeded* the digest savings on this one file. This isn't necessarily a net loss (Haiku tokens are far cheaper than whatever drives the main session), but it's exactly the signal spec §9 designed `skim stats` to surface, and it's now measurable for the first time (I6 fixed it). **Recommended follow-up once on GitHub:** run `skim stats` against a real multi-day session and consider raising `read_max_lines`/`read_max_bytes` if the worker-cost side dominates — the current defaults (300 lines / 60KB) may be too aggressive for Haiku's actual summarization cost on typical files.

## What's pending (do this first in a new session)

1. **Optional: give the final one-liner (point 4 above) a second set of eyes.** It's already controller-verified (build/vet/test green, the fix matches the exact pattern the earlier fix used, `isSkimCommand` already handles `cat`) but was never sent through a fresh reviewer subagent, and wasn't empirically checked against a real spaced-path binary copy the way the Bash-hook side was. If you want the same bar the other three fix waves got: build a copy of `plugin/bin/skim` at a path containing a space, run `read-hook` against an oversized file, and confirm the "force full file" suggestion in the deny reason is properly quoted — then optionally dispatch one scoped reviewer against just this commit. Given the size and nature of the change, this is very likely unnecessary busywork, but it's the one place this branch's rigor is thinner than everywhere else.
2. **Merge.** Once you're satisfied with (1) — or decide it's not needed — invoke `superpowers:finishing-a-development-branch` to merge `skim-impl` into `main`. This repo currently has *no PR open*; `skim-impl` is a plain feature branch pushed alongside `main`. Check `git status`/`git push` on both branches first — there may be commits on `skim-impl` not yet pushed to `origin` if this note was written before the final push (check the push log / just run `git push` again, it's a no-op if already current).
3. **If a fresh review turns up anything else:** per the SDD process, there is no open-ended fix-wave budget left for this branch — adjudicate each finding yourself (park with a ruling, or fix the load-bearing ones and ledger the decision), the same way the earlier rulings in this doc were made. Four fix rounds (fence-fix + 3 more) have already happened; the bar for another should be "this is load-bearing," not "this is nicer."
4. **The user's separate, deferred request:** turn the README (or a real GitHub Pages site — they explicitly chose "real GitHub Pages site" with shields.io badges when asked) into a nicer landing page with icons/images, once the code is confirmed on GitHub. This was explicitly deferred until after this push. Needs: badges (shields.io, no image files), and a decision on `docs/index.html` + Jekyll vs. a plain root-level Pages config, since this repo has no such setup yet.

## Deferred minors (not blocking, real follow-up value — from every task's review + the final review)

Grouped by area, for whenever someone circles back to polish:

- **config:** no test for the malformed-JSON→`Default()` fallback path.
- **detect:** `regexp.MustCompile` in `globToRegexp` could use a comment on why it's panic-safe; `FileSize` reads the whole file even after both thresholds are already exceeded (could early-exit); `**/` in glob patterns compiles to `.*` rather than `(.*/)?`, so `**/go.mod` also matches e.g. `/x/cargo.mod` — harmless with the shipped defaults, surprising for user-supplied globs.
- **worker:** timeout error message can misattribute the configured duration when a shorter parent context deadline actually fired; no `cmd.WaitDelay` hardening against a hypothetical hung child; a parent-context `Canceled` produces an uninformative bare error; no `\r\n` handling in the fence-strip (only `\n`); a narrow theoretical edge case where a malformed response with no real closing fence and stray trailing backticks in a JSON string could be mis-stripped (requires an already-broken response).
- **cache:** `Sweep` silently ignores individual `os.Remove` failures (best-effort by design).
- **handler:** degrade-open test coverage is uneven across branches (only the worker-error branch is directly tested per hook; `ParseFileMap`-error/`CacheKey`-error/`readCapped`-error/cache-hit-skips-worker aren't each individually tested, though they're structurally identical one-liners); a local variable named `max` shadows the Go 1.21 builtin in `read.go`; several tests discard `os.WriteFile`'s error return.
- **runner:** `Deps.Now` field is unused (kept for interface stability per spec); `os.MkdirAll`/`os.Create` errors aren't wrapped with context; coverage gaps on `argv==0`, the `ParseRun`-error fallback path, and ring-buffer trimming under a write larger than the 16KB cap; an empty `.log` file is created even if the child process fails to start.
- **cli:** `Stats`'s per-tool output iterates a Go map (non-deterministic ordering — sort the keys); `Doctor`'s `defer f.Close()` inside an `if` block defers to function return rather than block exit (harmless here); `Config`'s pretty-print swallows a `json.MarshalIndent` error (can't actually fail for this struct); `clearCache` under-counts silently on a permission failure; thin test coverage on `--clear-cache`, `set model`, `set disabled`, the Doctor INACTIVE line, and "rg missing still exits 0".
- **README:** the "invalid config" kill-switch bullet undersells the full scope of what `Validate()` checks; "last few errors" vs. `Doctor`'s actual "last 5"; the recursion guard isn't listed in the README's "kill switches" enumeration (it's documented separately, matching the spec's own §4.0/§7 split — not wrong, just easy to miss).

## How this was built (for anyone curious about process, not just code)

This was built via `superpowers:brainstorming` → `superpowers:writing-plans` → `superpowers:subagent-driven-development`: a controller session dispatched a fresh implementer subagent per task (mostly Haiku for mechanical transcription tasks, Sonnet for multi-file orchestration/integration tasks, Opus for the whole-branch reviews and the final fix wave), with a task-scoped spec+quality review after every task and a broad whole-branch review at the end. Two implementer subagents hit session rate-limits mid-task (Task 14's wiring, and the final fix wave) and were resumed rather than restarted — their in-progress work was inspected read-only first (build/vet/test) before resuming, and both turned out to be functionally complete, just uncommitted.

The single most valuable moment in the whole process was a manual, real-API smoke test (not a unit test) that caught the fence-wrapping bug — the entire fake-`claude`-based test suite was green while the product silently did nothing against the real model. The final review's own recommendation (Recommendation #3 in its report, referenced above) was to institutionalize this as a `make smoke` target gated on a real API key, run before every release. That target does not exist yet — worth adding.
